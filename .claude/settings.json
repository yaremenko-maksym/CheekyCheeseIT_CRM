{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('tool_input',d).get('command',''))\" 2>/dev/null || true); case \"$CMD\" in *grep*|*rg\\ *|*ripgrep*|*find\\ *|*fd\\ *) echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"MCP first: ast-grep (find_code/find_code_by_rule) for symbol/pattern search; context7 for library docs (NestJS/TanStack/Zod); postgres MCP for DB schema inspection instead of reading schema.ts; eslint MCP to pre-check code before pnpm lint. All are faster and token-efficient than grep/find.\"}}' ;; esac"
          }
        ]
      }
    ]
  }
}
