# task-junior-ux-3-cleanup — junior UX рефактор, фаза 3: чистка Финансы/Документы/Профиль

## Агент: coder · branch: feature/junior-ux-cleanup · Предпосылки ✅: фазы 1-2 в main (#171, #172)

## Источник: спека §4 (Финансы/Документы/Профиль) + деферы ревью #172 (ниже)

## Деферы ревью #172 — резолвить в этой задаче (documented-defer, не пересматривать)

1. **Dedup access-хелперов (api):** `hrCanAccessProject` (projects, hr-contact) частично дублирует
   `getHrSeniorIds` (users-access) — вынести единый helper. ЗАОДНО закрыть pre-existing LOW:
   при выборке команд HR фильтровать `leftAt` (уволившийся из команды HR не должен считаться активным).
   Регрессия: hr-contact integration тесты (HC-1..5) и users-access тесты остаются зелёными.
2. **`ContractMeDto` (shared):** сейчас inline в `apps/web/app/routes/crm/project.tsx` — вынести
   в packages/shared (Zod-схема + тип), переиспользовать в хабе.
3. **Легенда desktop-layout (web):** `/crm/legend` на ≥1024px — 2-колоночный grid
   (персона | cover story), журнал на всю ширину ниже — по дизайн-спеке docs/design/junior-hub.md §4.2
   (сейчас stacked).

## Конкретные изменения

1. **Финансы (JUNIOR-вью):** только свои SALARY-приходы с контекстом: проект · период · статус · сумма;
   человеческая расшифровка статусов и TX hash (тултип/легенда). Ни ставки, ни распределений, ни чужих данных.
2. **Документы:** только релевантные джуну категории (резюме, сканы, свой контракт, инвойсы зарплат);
   убрать disabled «Архив»; поправить неверный subtitle; недоступные категории не показывать «сломанными».
3. **Профиль:** свои данные/реквизиты/контракт/онбординг; убрать пути к чужим профилям.

## AC

1. Финансы JUNIOR: список только своих SALARY с контекстом; расшифровка статусов отображается. E2E.
2. Документы JUNIOR: нет «Архива»/disabled-категорий, subtitle корректен. E2E + скриншот.
3. Профиль JUNIOR: только своё; переходы на чужие профили отсутствуют в junior-UI. E2E.
4. Другие роли не задеты (регрессия E2E зелёная). Локальный гейт перед push; `ac_verified` + `vision:`.
