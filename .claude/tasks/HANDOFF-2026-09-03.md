# Handoff 2026-09-03 — состояние на момент, когда лимиты сессии подходят к концу

Читать первой в следующей сессии. Всё ниже — факты на момент записи; проверять `gh pr list`,
`git ls-remote`, а не верить этому файлу.

## Смёржено сегодня (main)

#628 спека черновиков · #629 дизайн-спека фильтра · #630 черновик проекта (сервер) · #631, #633
бэклог · #632 список scope'ов · #634 audit gate · #635 runbook гейта зависимостей · #636 страж
каталогов (переменные) · #637 гейт мутаций отдельным заданием · #639 бэклог copy-audit · #640
бэклог CI · #641 фильтр docs-only + мёртвая строка шарда · #642 матрица гейта по пакетам.

Branch protection: `Mutation Gate` добавлен в обязательные контексты (2026-09-03).

## В работе (агенты, ветки)

| Ветка                                | Что                                                                                | Состояние                                                                                                                        |
| ------------------------------------ | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `feat/user-emails-dual-login` (#623) | 3 находки текста + **34 выживших мутанта на полном диффе**                         | кодер; после — `copy-reviewer` r5, затем мерж. Оси: security APPROVE (7 раундов + SR-M-17), code APPROVE, spec PASS, design PASS |
| `feat/project-status-filter-ui`      | позиция 4, интерфейс черновиков                                                    | кодер; задание `.claude/tasks/task-project-status-filter-ui.md`; после — designer Mode B, copy, spec, security                   |
| `infra/mutation-gate-progress`       | прогресс-репортер + `ignoreStatic` (PR only) + **починка ночного** + шаблон алерта | DevOps; **`ignoreStatic` не мержить, пока ночной не показал зелёный полный сбор**                                                |
| `feat/signal-plus-service`           | Signal «+», шаг 1 (сервис + тесты)                                                 | кодер; задание `.claude/tasks/task-signal-plus-service.md`; PR не открывать до «ок» владельца                                    |

## Решения владельца сегодня (не пересматривать)

- Фокус — уведомления и подтверждения; остальное в бэклог; CI-оптимизации — безопасный минимум (сделан).
- `ignoreStatic: true` на PR-гейте, ночной прогон — полный. **Ночной был красным с 2026-08-15**
  (временной порог 250 мс в самопроверке) — сначала чинить, потом включать.
- Signal-сервис: опоздание допустимо до **08:00**, затем письмо на `yaremenkomaksym99@gmail.com`
  и останов на день (передача человеку; потолок 10:00 отменён). GPG-проверка релизов — принята.
  Native-сборка на сервере, JVM требует JRE 25.
- Бэклог: аудит текста лендинга и CRM — первым шагом следующей задачи по мультиязычности (#639).

## Ждёт владельца

- Вывод трёх серверных команд: `systemctl is-enabled docker; docker compose ls; df -h /opt/crm /var/lib/docker` — нужно **до шага 2** Signal-сервиса.
- DMARC-запись `_dmarc` `v=DMARC1; p=none;` в Cloudflare (Resend → Domains → Auto configure) — без неё письмо-алерт рискует спамом. Проверка: `dig +short TXT _dmarc.cheekycheese.tech`.

## Дальше по плану уведомлений

Позиция 5 — ожидающая доля (`.claude/tasks/task-pending-share.md`, готов; решение владельца:
проектный и базовый проценты ждут подтверждения, командный — нет). Позиции 6–7 — типы
уведомлений и производители, письма/настройки — задания ещё не написаны; спека §7–§8.

## Уроки дня (записаны в бэклог 130–136 и память)

Механизмы, уверенно сообщающие о том, чего не проверяли: 12 ложных комментариев, 4 разошедшихся
перечисления, страж каталогов «проверил» без проверки, ночной гейт красный 20 дней с алертом в
репозиторий, куда никто не смотрит. Правило: статус — не свидетельство; свидетельство — что
именно выполнилось.
