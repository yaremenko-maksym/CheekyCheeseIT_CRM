Throwaway file for a CI probe (task-infra-split-mutation-gate, PR #637). Verifies
that the new `mutation` job's docs-only short-circuit fires correctly. This PR is
never merged — closed and branch deleted right after the check.
