# Test environment — loaded automatically by vitest for test workers.
# Integration tests run against crm_qa (NOT crm_db).
# To create: createdb crm_qa && DATABASE_URL=...crm_qa pnpm --filter @crm/api db:push
DATABASE_URL=postgresql://crm_user:password@localhost:5432/crm_qa
