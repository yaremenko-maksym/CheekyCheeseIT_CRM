ECC_VERSION_TAG=v2.0.0-rc.1
ECC_VERSION_SHA=928076cc08cbb31e8549cea2883b4f51811de1c8
ECC_PIN_DATE=2026-05-31
ECC_PIN_ADOPTED_BY=Architect dispatch (Phase 0 master ADR + Phase 1 bootstrap)
ECC_REPO_URL=https://github.com/affaan-m/ECC
ECC_CHERRY_PICKS=
  # Format: <sha> <date> <reason>
  # (empty until first cherry-pick — e.g. security hotfix)
ECC_NEXT_SYNC_DATE=2026-12-XX
  # Q4 2026 quarterly sync post-migration

# Install method (Phase 1)
ECC_INSTALL_METHOD=manual-cherry-pick
ECC_INSTALL_PROFILE=developer+security-selective
ECC_INSTALL_LAYOUT=root
  # Reason: install-apply.js targets ./.claude/ which conflicts with existing
  # project .claude/hooks/ + .claude/settings.json. Manual cherry-pick to root
  # paths (agents/, skills/ecc/, hooks/, rules/ecc/, manifests/, mcp-configs/)
  # gives ECC-native layout per ADR Q6=A while preserving existing system.

# Selective additions (per ADR Q4 = Option A, security profile picks)
ECC_SECURITY_ADDITIONS=defi-amm-security,evm-token-decimals,security-review,security-scan,security-bounty-hunter,security-reviewer-agent
  # wallet-address-validation NOT present in v2.0.0-rc.1 — skipped
  # All other security additions verified present in pinned catalog

# Fallback (if rc1 caused install blocking issue — NOT triggered in Phase 1)
ECC_FALLBACK_TAG=v1.10.0
ECC_FALLBACK_SHA=846ffb75da9a5f4e677d927af1ad4a1951652267
